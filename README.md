# PlantVillage Classification API Service

An asynchronous FastAPI environment wrapper managing a Keras `.h5` model.

## 🚀 Local Deployment Setup

1. **Environment Initialization:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows use: venv\Scripts\activate
